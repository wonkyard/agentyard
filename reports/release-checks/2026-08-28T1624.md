# Release Check — 2026-08-28 16:24

Branch: `v0.4-run` (not yet committed at review time; diff reviewed with
`git diff HEAD` + new untracked files)

Files Changed: 9 modified, 3 added
(`shared/winWrap.js`, `extension.js`, `webview/js/live.js`, `webview/js/model.js`,
`package.json`, `scripts/sanity.mjs`, `CHANGELOG.md`, `README.md`, `.vscodeignore`;
added `scripts/make-zombie-fixture.mjs`, `dev-data/zombie-session.jsonl`,
`reports/`)

## Findings

- [Info] Security fix is the core of this diff: the Run view no longer invokes
  `cmd.exe` for a `.cmd`/`.bat` launcher — it resolves the shim and spawns the
  real executable with `shell:false`. Verified on-device against the real
  installed `claude.cmd` and with a hostile-prompt spawn test.
- [Info] `dev-data/zombie-session.jsonl` — checked synthetic: no `C:\Users\…`
  paths, no real session id, `velog`/handle scrubbed. Sanity section 0 asserts
  this on every run. Excluded from the vsix via `.vscodeignore`.
- [Low] `reports/` is now tracked in this repo for the first time (build notes,
  no secrets, no pricing — this repo is public and its own subject). Acceptable;
  flag for `project-lead` if a `reports/.gitignore` is preferred later.
- No exposed secrets. No debug leftovers (`console.log` only in `scripts/` test
  output, as before). No stray `node_modules` / `.env` / build output staged
  (`*.vsix` stays gitignored). No syntax errors (`node -c` on all changed JS +
  `npm run sanity`). Scope matches the FAIL brief's four items exactly — no
  unrelated changes.

## Tests

`npm run sanity` → `111 PASS`, `ALL PASS`.
New coverage: spawn-level Windows launcher regression (verbatim argv + no
injected process), `parseCmdShim` cases, zombie staleness horizon (synthetic +
captured force-close log).

## Verdict
PASS
