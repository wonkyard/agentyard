# Release Check — 2026-08-29 12:17

Branch: `v0.5-terminal` (off `main` @ `068121c`)
Scope reviewed: `git diff HEAD` — v0.5 embedded terminal (spec
`../company/reports/TOOL-20260828-1008/v0.5-terminal.md`).

Files Changed: 15 modified + 6 added (`webview/js/term.js`, 5 vendored xterm assets)

## Checklist

- **Exposed secrets**: none. No API keys/tokens/passwords. node-pty runs the
  user's own `claude` with their existing CLI auth — no key handling added.
- **Debug leftovers**: none. No `console.log`/`debugger`/`TODO` in the diff.
- **Unintended files**: `vsce ls` reviewed (140 files, 3.22 MB). Ships
  `webview/vendor/xterm*` (vendored, like sql.js) and the node-pty package's
  runtime bits only — `.pdb` debug symbols (~28 MB), `src/`, `scripts/`,
  `third_party/`, `binding.gyp` are all excluded via scoped `.vscodeignore`
  negations. No `.env`, no `state/company.db`, no `dev-data/real/`. `*.vsix` is
  gitignored. `node_modules/` gitignored.
- **Obviously broken code**: none. `node -c` clean on every changed JS file;
  `npm run sanity` = ALL PASS (136 checks); node-pty spawn + `killTree`
  smoke-tested live on win32 (conpty).
- **Scope creep**: none. Office view / pixel scene / live hooks / DB reader /
  zombie horizon untouched. Headless `claude -p` path (`buildClaudeArgs`,
  `streamJson`, `run.js`) preserved as the `runView: "headless"` fallback.
- **Missing tests**: covered. `scripts/sanity.mjs` +29 checks: interactive argv
  has no `-p`/`--output-format`, permission-mode/extraArgs mapping, pty resolves
  through the winWrap no-cmd.exe path with an argv array, node-pty require is
  guarded, pty killed on panel dispose, headless auto-fallback, xterm vendored
  with no CDN, new config/command/version. `reports/eng/2026-08-29.md` carries
  the run result.

## Findings

- [Low] macOS/Windows-arm64 prebuilt binaries are not in the tree — the sandbox
  blocks npm install scripts, so only what ships in the npm tarball is present
  (`build/Release/*` win32-x64, `prebuilds/linux-*`). On other platforms the Run
  view degrades to the headless feed (the required graceful-degradation path).
  A full multi-platform publish needs `prebuild-install` per target on a
  networked machine first — documented in `CLAUDE.md` (Maintenance section) and
  `README`. Not a blocker for this Gate; flag for the publish step.
- [Low] `lib/*.test.js` (~10 tiny files) ride along in the packaged node-pty —
  vsce force-includes anything matching a `!` negation, so they can't be
  re-excluded after `!.../lib/**/*.js`. ~20 KB, harmless.

## Verdict
PASS
