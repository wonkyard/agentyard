# Eng — 2026-08-29

Backlog item: v0.5 — replace the Run feed with a real embedded terminal

Changed:
- `package.json` — version 0.5.0; `@homebridge/node-pty-prebuilt-multiarch@0.14.1`
  runtime dep; `@xterm/xterm@5.5.0` + `@xterm/addon-fit@0.10.0` dev deps (vendor
  only); new `agentyard.runView` config; `agentyard.openClaudeTerminal` command;
  reworded `claudeExtraArgs` / `claudePermissionMode` descriptions.
- `scripts/vendor.mjs` — also vendors xterm.js / xterm.css / addon-fit + LICENSE
  files into `webview/vendor/` (pins in package.json, no CDN).
- `shared/claudeArgs.js` — new pure `buildInteractiveClaudeArgs()` (no `-p`, no
  `--output-format`; `--permission-mode` only when not default; extraArgs
  verbatim). Headless `buildClaudeArgs()` untouched.
- `extension.js` — guarded `require` of node-pty (`nodePty` / `nodePtyError`);
  `resolvePtyClaude()` (candidateCommands + winWrap shim resolution, no cmd.exe);
  `ptyKillHandle()` adapter for `killTree`; new `TerminalRun` class (one pty per
  panel, kept in the extension host, 256 KB replay buffer for webview reload,
  `killTree` on New thread / panel dispose / deactivate); `openClaudeTerminal()`
  fallback command; `getHtml` computes effective `runView` (auto-falls to
  headless when node-pty is missing) and passes `runView` / `runViewRequested` /
  `ptyAvailable` into `AY_CONFIG`; xterm vendor `<script>`/`<link>` + `js/term.js`
  added; `term` message routing; provider wires + disposes `this.term`.
- `webview/js/term.js` (new) — xterm.js surface, FitAddon, `ResizeObserver`;
  forwards input/resize, writes pty data, spawns on first on-screen fit (not on
  webview load), New thread button, one-line fallback notice.
- `webview/js/adapter.js` — `term*` methods + `onTerm` listener routing (vscode);
  no-op stubs + `termSupported:false` (browser).
- `webview/js/main.js` — calls `AY.term.init()`; re-fits xterm on view switch.
- `webview/index.html` / `webview/css/style.css` — `#run-term` / `#run-notice` /
  `#run-term-foot`; browser default `AY_CONFIG.runView = "headless"`.
- `.vscodeignore` — scoped negations to ship node-pty runtime bits only.
- `CLAUDE.md` — Maintenance section: node-pty Electron-ABI rebuild step.
- `CHANGELOG.md` / `README.md` — v0.5 Run view.

Spec §2 (markdown in HTML-rendered assistant text): **closed, no renderer added.**
After §1 the terminal renders its own markdown. Audited the other surfaces: the
Office scene (`render.js`) draws to `<canvas>`, `main.js showPanel()` builds
innerHTML only from `esc()`-escaped status/note strings, `live.js` has no DOM
output. No assistant text is rendered as HTML anywhere, so a sanitising markdown
renderer would be dead code.

Tests:
- `npm run sanity` → `ALL PASS` (136 PASS, 0 FAIL); +29 new checks for the
  terminal path (interactive argv shape, no-cmd.exe pty resolution, guarded
  require, kill-on-dispose, headless auto-fallback, xterm vendored / no CDN,
  runView/command/version manifest wiring).
- Live smoke test: `nodePty.spawn(node, [...argv])` on win32 → `onData` fired,
  `killTree(ptyKillHandle(pty))` returned `true` and reaped the tree.
- `npx vsce package` → `agentyard-0.5.0.vsix` (140 files, 3.22 MB). `vsce ls`
  reviewed: win32-x64 `build/Release/*.node` + `prebuilds/linux-*` present, no
  `.pdb` / `src` / `third_party` / `.env` / `state`.

Done-when check: met for win32-x64 and linux-x64 (prebuilt binary present, sanity
green, no orphan process after kill, headless fallback + notice verified in code
+ tests, vsix builds). macOS / win-arm64 rely on the headless fallback until
their prebuilds are fetched at publish time (sandbox blocks npm install scripts)
— documented in CLAUDE.md + README.

## Status
READY FOR RELEASE CHECK
