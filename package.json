{
  "name": "agentyard",
  "displayName": "Agentyard",
  "description": "A pixel-art view of your agent company, in the VS Code panel.",
  "version": "0.4.1",
  "publisher": "wonkyard",
  "license": "MIT",
  "icon": "media/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/wonkyard/agentyard.git"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "panel": [
        {
          "id": "agentyard",
          "title": "Agentyard",
          "icon": "media/panel-icon.svg"
        }
      ]
    },
    "views": {
      "agentyard": [
        {
          "id": "agentyard.office",
          "name": "Agentyard",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "agentyard.focus",
        "title": "Focus Panel",
        "category": "Agentyard"
      },
      {
        "command": "agentyard.enableLiveMode",
        "title": "Turn On Live Mode",
        "category": "Agentyard"
      },
      {
        "command": "agentyard.disableLiveMode",
        "title": "Turn Off Live Mode",
        "category": "Agentyard"
      }
    ],
    "configuration": {
      "title": "Agentyard",
      "properties": {
        "agentyard.dbPath": {
          "type": "string",
          "default": "state/company.db",
          "description": "Path to company.db relative to the first workspace folder."
        },
        "agentyard.agentsGlob": {
          "type": "string",
          "default": ".claude/agents",
          "description": "Folder that holds the department agent .md files, relative to the first workspace folder."
        },
        "agentyard.pollSeconds": {
          "type": "number",
          "default": 3,
          "description": "How often to re-read the DB and agent files."
        },
        "agentyard.idleSeconds": {
          "type": "number",
          "default": 30,
          "description": "An agent with no tool activity for this many seconds is shown as idle."
        },
        "agentyard.staleMinutes": {
          "type": "number",
          "default": 15,
          "description": "A live agent that never reported finishing and has had no activity for this many minutes is treated as a zombie (its VS Code was force-closed mid-run) and dropped from the scene. 0 disables the check."
        },
        "agentyard.maxSpritesPerRoom": {
          "type": "number",
          "default": 8,
          "description": "A room busier than this shows the first N agents and a \"+N more\" tile."
        },
        "agentyard.claudePath": {
          "type": "string",
          "default": "claude",
          "description": "Command used to run the Claude Code CLI for the Run view. A bare name is looked up on PATH; on Windows Agentyard also tries claude.exe and claude.cmd. Uses your existing CLI login — no API key."
        },
        "agentyard.claudeExtraArgs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra arguments appended verbatim to each `claude -p` run, e.g. [\"--allowedTools\", \"Read Edit Bash(npm test)\"]. Headless runs cannot answer permission prompts, so pre-allow the tools you want."
        },
        "agentyard.claudePermissionMode": {
          "type": "string",
          "enum": [
            "default",
            "acceptEdits",
            "plan",
            "bypassPermissions"
          ],
          "default": "default",
          "description": "Passed as --permission-mode for Run-view runs. Left as `default`, no flag is added. `bypassPermissions` skips all permission checks — only use it in a workspace you trust."
        }
      }
    }
  },
  "scripts": {
    "vendor": "node scripts/vendor.mjs",
    "icon": "node scripts/make-icon.mjs",
    "demo-data": "node scripts/make-demo-data.mjs",
    "dev": "node scripts/dev-server.mjs",
    "sanity": "node scripts/sanity.mjs"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.2.0",
    "sql.js": "1.10.3"
  }
}
